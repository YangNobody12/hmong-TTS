# 🗣️ F5TTS Hmong Text-to-Speech Model

คู่มือการติดตั้งและใช้งานโมเดลแปลงข้อความเป็นเสียงพูด **F5TTS** สำหรับภาษาม้ง (Hmong) บนเครื่องของคุณเอง

---

## 📋 ข้อกำหนดเบื้องต้น

เพื่อให้ระบบสามารถทำงานได้อย่างเต็มประสิทธิภาพ ควรมี:

* 💻 เครื่องคอมพิวเตอร์ที่มีการ์ดจอ (GPU) จาก **NVIDIA**
* 🖥️ ระบบปฏิบัติการ **Windows, macOS หรือ Linux**
* 💾 พื้นที่ว่างบนดิสก์อย่างน้อย **2–3 GB**
* 🐳 ติดตั้ง **Docker Desktop**

> ⚠️ *หากไม่มี GPU สามารถใช้งานได้ แต่ประสิทธิภาพการสร้างเสียงจะช้าลง*

---

## 🚀 ขั้นตอนการติดตั้ง

### 1️⃣ ติดตั้ง Docker Desktop

#### ✔️ สำหรับ Windows:

* ดาวน์โหลด Docker Desktop จากเว็บไซต์ทางการ
* ติดตั้งและรีสตาร์ทเครื่องคอมพิวเตอร์
* เปิด Docker Desktop และรอจนสถานะเป็น **"Running"**

#### 🍎 สำหรับ macOS:

* ดาวน์โหลดจากเว็บไซต์ทางการ
* ลาก `Docker.app` ไปยังโฟลเดอร์ **Applications**
* เปิดโปรแกรมและรอจนสัญลักษณ์ทะเลเป็นสีเขียว

---

### 2️⃣ เปิด Docker Desktop

* เปิดโปรแกรมจาก **Start Menu (Windows)** หรือ **Applications (macOS)**
* รอจนเห็นไอคอน Docker ทำงาน (สีเขียว)

---

### 3️⃣ ดาวน์โหลดโมเดล F5TTS

เปิด **Command Prompt / Terminal** และรันคำสั่ง:

```bash
docker pull yangnobody/f5tts-hmong:v1.0
```

> ⏳ *หมายเหตุ: การดาวน์โหลดอาจใช้เวลาหลายนาทีขึ้นอยู่กับความเร็วอินเทอร์เน็ต*

---

### 4️⃣ เปิดใช้งานโมเดล

รันคำสั่ง:

```bash
docker run -p 7860:7860 --gpus all yangnobody/f5tts-hmong:v1.0
```

---

## 🌐 การใช้งาน

1. เปิดเว็บเบราว์เซอร์ (Chrome, Firefox, Edge ฯลฯ)
2. ไปที่ URL:

```
http://localhost:7860
```

3. พิมพ์ข้อความภาษาม้งที่ต้องการแปลงเป็นเสียง
4. คลิกปุ่ม **Generate** เพื่อสร้างเสียง
5. ดาวน์โหลดไฟล์เสียงได้ตามต้องการ

---

## 🔧 คำสั่งเพิ่มเติม (Optional)

### 📌 ตั้งชื่อ Container:

```bash
docker run -p 7860:7860 --gpus all --name f5tts-hmong yangnobody/f5tts-hmong:v1.0
```

### ⏹ หยุดการทำงาน:

```bash
docker stop f5tts-hmong
```

### ▶️ เริ่มใหม่:

```bash
docker start f5tts-hmong
```

---

## ❓ ปัญหาที่พบบ่อย

#### ❌ Docker ไม่พบ GPU

* ตรวจสอบว่าติดตั้ง **NVIDIA Container Toolkit** แล้ว
* บน Windows ต้องมี **WSL 2** และใช้ Driver ล่าสุด

#### ❌ พอร์ต 7860 ถูกใช้งานแล้ว

ใช้พอร์ตอื่นแทน:

```bash
docker run -p 7861:7860 --gpus all yangnobody/f5tts-hmong:v1.0
```

#### ❌ ความจำไม่เพียงพอ

* ปิดโปรแกรมที่ใช้ GPU สูง เช่นเกมหรือโมเดล AI อื่น ๆ
* ตรวจสอบ VRAM ของการ์ดจอ

---

## 📞 การสนับสนุน

💬 หากพบปัญหา สามารถตรวจสอบเพิ่มเติมได้ที่:

* Docker Documentation
* NVIDIA Driver และ CUDA Toolkit

📌 ผู้พัฒนาและดูแลระบบ F5TTS Hmong โดย **Pakorn Archakeeree**
